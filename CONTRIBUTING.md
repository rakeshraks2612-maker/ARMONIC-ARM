# Contributing to ARMONIC

We welcome contributions from the open-source hardware community! 
To contribute:
1. Fork the repository.
2. Ensure your patches compile on Arm64 architecture (e.g., Apple Silicon or AWS Graviton).
3. Run the CI pipeline locally using `python3 armonic/run.py --config config.yaml`.
4. Submit a Pull Request with attached APX profiling logs.
